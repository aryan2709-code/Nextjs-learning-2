

export default function Home() {
  return <div>
    Hi there
  </div>
}

// assignment : creating a signup and signin page and making sure that the layout only applies to both of these pages and doesn't apply to any other page (e.g user)